# Codex Log - 2026-08-31 22:10 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-31 22:10 CST
- End: 2026-08-31 22:10 CST

## Task

分析 `code7.cu` 相对 `code6.cu` 性能明显变慢的原因，并判断当前 swizzle / XOR 思路是否有效。

## Files Read

- `cases/04_large_30k/code6.cu`
- `cases/04_large_30k/code7.cu`
- `logs/codex-20260831-1314.md`
- `logs/codex-20260830-0119.md`

## Commands

- `sed -n '1,260p' cases/04_large_30k/code6.cu`
- `sed -n '1,260p' cases/04_large_30k/code7.cu`
- `diff -u cases/04_large_30k/code6.cu cases/04_large_30k/code7.cu | sed -n '1,260p'`
- `/usr/local/cuda-13.1/bin/nvcc -c cases/04_large_30k/code6.cu -O2 -std=c++17 -arch=sm_80 -Xptxas -v -o /tmp/code6.o`
- `/usr/local/cuda-13.1/bin/nvcc -c cases/04_large_30k/code7.cu -O2 -std=c++17 -arch=sm_80 -Xptxas -v -o /tmp/code7.o`

## Findings

- `code6.cu` 和 `code7.cu` 都编译为 `128 registers/thread`，所以这次性能差异不是寄存器数造成的。
- `code7.cu` 比 `code6.cu` 多了 block 级 XOR swizzle、每个 8x4 / 4x8 block 的 `+1` padding，以及更大的 shared staging footprint。
- 这些变化没有改变 `wmma::load_matrix_sync` 真正看到的 fragment 访问形态，反而增加了地址计算和 shared footprint。
- `code6.cu` 之前的 packed block layout 更贴近 WMMA fragment 的访问模式，因此更像是“已经够好”的版本。

## Notes

- 当前 `code7` 的慢，说明这版 swizzle 实现大概率没有打中真实瓶颈。
- 更合理的下一步是先用 NCU 复核 `code6` 的 shared bank conflict / issue bottleneck，再决定是否需要更精确的 lane-level layout，而不是继续扩大 block 级 swizzle。
