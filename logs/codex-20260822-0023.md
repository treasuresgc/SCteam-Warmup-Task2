# Codex Log - 2026-08-22 00:23 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-22 00:23 CST
- End: 2026-08-22 00:23 CST

## Task

检查 `cases/04_large_30k/report16` 里为什么没有 CUDA kernel data，并核对 `cases/04_large_30k/code3.cu` 的实现。

## Findings

- `report16.sqlite` 里的 `cudaLaunchKernel_v7000` 返回值是 `701`，对应 `cudaErrorLaunchOutOfResources`。
- `report16.sqlite` 没有 `CUPTI_ACTIVITY_KIND_KERNEL` 表，因此 kernel 根本没有成功启动。
- 当前 `cases/04_large_30k/code3.cu` 里 `ThreadsY` 被写成了 `16`，与 `CtaM = 128` 和 `ThreadM = 16` 不匹配。
- 该配置会让一个 block 试图覆盖 `256 x 128` 的输出范围，同时 `ptxas` 显示每线程 248 registers，512 线程/block 会超出可用寄存器预算。

## Files Changed

- `cases/04_large_30k/code3.cu`
  - 将 `ThreadsY` 从 `16` 改回 `8`，恢复 `32 x 8` block 和 `128 x 128 x 16` CTA tile 的一致配置。

## Commands

- `sqlite3 cases/04_large_30k/report16.sqlite ...` on `CUPTI_ACTIVITY_KIND_RUNTIME`, `CUPTI_ACTIVITY_KIND_MEMCPY`, `PROCESSES`, `TARGET_INFO_GPU`, `StringIds`
- `rg -n "cudaErrorLaunchOutOfResources|cudaErrorInvalidConfiguration|cudaErrorLaunchFailure|= *701" /usr/local/cuda-13.1/include -g '*.h'`
- `sed -n '1,160p' /usr/local/cuda-13.1/include/driver_types.h`
- `/usr/local/cuda-13.1/bin/nvcc -c code3.cu -O2 -std=c++17 -arch=sm_80 -Xptxas=-v -o /tmp/task2_code3_fixed.o`

## Verification

- `code3.cu` 重新编译成功。
- `ptxas` 报告 `248 registers`, `32768 bytes smem`, `0 spill stores/loads`。

## Notes

- `report16` 的问题是 launch 失败，不是 kernel 运行中崩溃。
- `report8` 仍然对应能成功启动 kernel 的版本。
