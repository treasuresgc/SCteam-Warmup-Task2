# Codex Log - 2026-08-31 13:14 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-31 13:14 CST
- End: 2026-08-31 13:14 CST

## Task

在当前回退后的 `cases/04_large_30k/code6.cu` 基础上新增 `code7.cu`，加入 shared-memory swizzle / XOR layout 优化，并更新构建脚本。

## Files Changed

- `cases/04_large_30k/code7.cu`
  - 新增基于 `code6.cu` 的 `code7`。
  - 保留双缓冲、`cp.async` 和 WMMA 计算路径。
  - 改为带 1 列 / 1 行 padding 的 packed block staging。
  - 在 block grid 上加入轻量 XOR swizzle，尝试降低 SMEM -> Tensor Core 的 bank conflict。
  - 加入 `__launch_bounds__(ThreadsPerBlock, 1)`，把寄存器占用压回 128 regs/thread。
- `cases/04_large_30k/build.sh`
  - 将 `code7.cu` 纳入构建循环。

## Commands

- `sed -n '1,260p' cases/04_large_30k/code6.cu`
- `sed -n '1,220p' cases/04_large_30k/build.sh`
- `/usr/local/cuda-13.1/bin/nvcc -c cases/04_large_30k/code7.cu -O2 -std=c++17 -arch=sm_80 -Xptxas -v -o /tmp/code7.o`
- `/usr/local/cuda-13.1/bin/nvcc cases/04_large_30k/gen.cpp cases/04_large_30k/code7.cu -O2 -std=c++17 -arch=sm_80 -I/usr/local/cuda-13.1/targets/x86_64-linux/include -L/usr/local/cuda-13.1/targets/x86_64-linux/lib -lcublasLt -lcublas -Xlinker -rpath -Xlinker /usr/local/cuda-13.1/targets/x86_64-linux/lib -o /tmp/task2_main7`

## Findings

- `code7.cu` 编译成功。
- `ptxas` 报告 `128 registers`, `32 bytes stack frame`, `0 spill stores`, `0 spill loads`。
- 链接 `gen.cpp + code7.cu` 成功，生成了 `/tmp/task2_main7`。
- 由于当前沙箱没有可用 NVIDIA driver / device，未做实际 GPU 运行或 NCU 复测。

## Notes

- 这版 `code7` 仍然属于 shared layout 级别优化，没有往更底层的 MMA/PTX 方向走。
- 后续如果要继续压 bank conflict，下一步应在有 GPU 的环境里跑 NCU，确认 1-way / 2-way conflict 是否真的改善。
