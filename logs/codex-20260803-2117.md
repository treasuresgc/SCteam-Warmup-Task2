# Codex Work Log

- Agent: Codex
- Start time: 2026-08-03 21:17 CST
- End time: 2026-08-03 21:17 CST

## Task

Measure `resident_blocks_per_SM` and add it to `GPU_CONFIG.md`.

## Files changed

- Updated `GPU_CONFIG.md`
- Added `logs/codex-20260803-2117.md`

## Commands run

- `rg -n "resident_blocks_per_SM|resident blocks|blocks_per_SM|occupancy|SM" Readme.md AGENT.md GPU_CONFIG.md logs/*.md`
- `rg -n "resident_blocks_per_SM|resident_blocks|blocks_per_sm" cases`
- `nl -ba GPU_CONFIG.md | sed -n '45,55p'`
- `nl -ba GPU_CONFIG.md | sed -n '100,108p'`
- `sed -n '24,60p' GPU_CONFIG.md`
- `sed -n '96,108p' GPU_CONFIG.md`
- `date '+%Y-%m-%d %H:%M:%S %Z'`

## Result

Added `resident_blocks_per_SM` to `GPU_CONFIG.md` as a documented device limit for Compute Capability 8.0:

- `resident_blocks_per_SM`: `32` (`Maximum number of resident blocks / SM`)

The value is taken from NVIDIA's official CUDA Programming Guide compute capability table for CC 8.0. The current sandbox still cannot run CUDA runtime device queries because GPU device nodes are not exposed, so this was filled from the official specification rather than a local runtime probe.

## Validation

Read back the edited `GPU_CONFIG.md` section to confirm the new row and explanatory note were inserted.

## Open issues

- No CUDA runtime probe was possible in this sandbox.
