# Codex Log 2026-08-06 21:18

- 任务：阅读项目文档，确认 `cases/03_medium_5k` 的 `build`、运行与 profiling 方式，以及 `code.cu` 是否需要改名。
- 读取文件：`Readme.md`、`AGENT.md`、`cases/03_medium_5k/build.sh`、`cases/03_medium_5k/gen.cpp`、`cases/03_medium_5k/code.cu`。
- 结论：
  - `cases/03_medium_5k/build.sh` 默认直接编译 `code.cu` 生成 `main`。
  - `./build.sh profile` 会先编译四个版本，再用 `nsys profile` 依次采集 `main`、`main2`、`main3`、`main4`。
  - 如果只想 profile `code.cu` 对应版本，直接对 `./main` 运行 `nsys profile` 即可。
  - 将 `code.cu` 改名为 `code1.cu` 不是必需；若改名，必须同步修改 `build.sh` 中的编译目标，否则构建会失败。
- 本次没有修改源码，也没有实际运行 build 或 profile 命令。
