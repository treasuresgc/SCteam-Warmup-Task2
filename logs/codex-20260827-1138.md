# Codex Log - 2026-08-27 11:38 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-27 11:38 CST
- End: 2026-08-27 11:39 CST

## Task

评估 `cases/04_large_30k/code5.cu` 的 `double -> double2` 向量化方案，并修改代码为 16-byte staging 路径，为后续 `cp.async` 做准备。

## Files Changed

- `cases/04_large_30k/code5.cu`
  - 保留现有 FP64 WMMA 计算路径。
  - 将 CTA tile 的 global-to-shared staging 改为 `double2` 向量化复制。
  - 增加偶数 tile / stride 的静态断言，保证 `wmma::load_matrix_sync` 的对齐前提。
  - 给 kernel 参数补了 `__restrict__`。

## Commands

- `date '+%F %T %Z'`
- `nl -ba cases/04_large_30k/code5.cu | sed -n '1,240p'`
- `/usr/local/cuda-13.1/bin/nvcc -c cases/04_large_30k/code5.cu -O2 -std=c++17 -arch=sm_80 -o /tmp/task2_code5.o`
- `/usr/local/cuda-13.1/bin/nvcc cases/04_large_30k/gen.cpp cases/04_large_30k/code5.cu -O2 -std=c++17 -arch=sm_80 -o /tmp/task2_main5`

## Findings

- 方案可行：当前 case 的 `m/n/k` 和 CTA tile 都是偶数，`cudaMalloc` 返回的设备指针也满足 16-byte 对齐，`double2` staging 不会破坏现有 WMMA 读取路径。
- 这一步只优化 staging，不改变 fragment 计算逻辑，因此是通往 `cp.async` 的合理前置。
- `code5.cu` 目前不是 `cuBLASLt` fast path，而是和 `code4.cu` 同类的 WMMA 实现；本次只改代码，不改文档描述。

## Notes

- 未运行完整大矩阵 benchmark；当前只做了编译和链接验证。
