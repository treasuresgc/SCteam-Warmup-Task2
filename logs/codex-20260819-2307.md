# Codex 2026-08-19 23:07

## 目标
核对 `cases/04_large_30k/code0.cu`、`code2.cu`、`build.sh` 与 `report0/report2` 的对应关系，并分析两份报告的时间差异。

## 已检查文件
- `AGENT.md`
- `Readme.md`
- `cases/04_large_30k/build.sh`
- `cases/04_large_30k/code0.cu`
- `cases/04_large_30k/code1.cu`
- `cases/04_large_30k/code2.cu`
- `cases/04_large_30k/code4.cu`
- `cases/04_large_30k/gen.cpp`
- `cases/03_medium_5k/build.sh`
- `cases/03_medium_5k/code2.cu`
- `cases/02_small_256/build.sh`

## 已运行命令
- `rg --files -g 'code0.cu' -g 'code2.cu' -g 'report0*' -g 'report2*' -g 'build.sh' -g 'main*' -g 'Makefile'`
- `sed -n ...` 查看上述源码和说明文件
- `sqlite3 cases/04_large_30k/report0.sqlite ...`
- `sqlite3 cases/04_large_30k/report2.sqlite ...`
- `sqlite3 cases/04_large_30k/report1.sqlite ...`

## 结论
- `build.sh` 会把 `code0.cu` 编成 `main0`，把 `code2.cu` 编成 `main2`。
- `report0.sqlite` 对应进程 `./main0`，`report2.sqlite` 对应进程 `./main2`。
- 两份报告里各自只有一个主要 kernel。
- 当前读到的计时结果显示 `main0` 的 kernel 约 `4.224 s`，`main2` 的 kernel 约 `40.546 s`。
- 这次没有做源码修改。
- 新查到的 `report1.sqlite` 对应 `./main1`，其 kernel 约 `22.445 s`，仍明显快于 `main2`。
- `main1` 和 `main2` 在同一 A800 80GB PCIe 上采集，launch 形态相同，瓶颈差异主要来自 kernel 内部资源占用和访存路径。
