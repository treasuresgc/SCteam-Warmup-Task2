# Codex Work Log

- Agent: Codex
- Start time: 2026-08-02 20:49 CST
- End time: 2026-08-02 20:49 CST

## Task

将当前 GPU 类型、显存、shared memory、SM 个数、每个 SM 的 core 个数和 L2 cache 大小写成 Markdown 文档放在项目根目录。

## Files changed

- Added `GPU_CONFIG.md`
- Added `logs/codex-20260802-2049.md`

## Commands run

- `conda run -n Matmul nvidia-smi --query-gpu=index,name,uuid,pci.bus_id,memory.total,memory.free,memory.used,compute_cap,mig.mode.current,driver_version --format=csv,noheader,nounits`
- `date '+%Y-%m-%d %H:%M:%S %Z'`
- `ls`

## Result

Created `GPU_CONFIG.md` in the repository root. The document records:

- two visible NVIDIA A800 80GB PCIe GPUs;
- per-GPU memory, UUID, bus ID, compute capability, MIG mode, and driver version from `nvidia-smi`;
- A800 / Ampere GA100 specification values for SM count, CUDA cores per SM, Tensor Cores, shared memory, and L2 cache;
- CUDA Toolkit 13.1 information;
- the current sandbox limitation where CUDA runtime cannot enumerate devices because `/dev/nvidia*` nodes are not visible.

## Validation

No build, CUDA kernel run, or profiling command was executed. The hardware inventory command completed successfully through `conda run -n Matmul nvidia-smi`.

## Open issues

- The current sandbox still cannot directly run CUDA runtime device-property queries. The detailed architecture values in `GPU_CONFIG.md` should be treated as specification values for NVIDIA A800 80GB PCIe / GA100 until verified with `cudaDeviceProp` in a runtime-visible environment.
