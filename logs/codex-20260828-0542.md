# Codex Log - 2026-08-28 05:42 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-28 05:42 CST
- End: 2026-08-28 05:43 CST

## Task

继续在 `cases/04_large_30k/code5.cu` 中加入 `cp.async` 和双缓冲。

## Files Changed

- `cases/04_large_30k/code5.cu`
  - 把 shared staging 改成 `__pipeline_memcpy_async`。
  - 增加 2-stage ping-pong shared buffer。
  - 保留现有 WMMA 计算路径，只改 global-to-shared 预取方式。
  - 改成动态 shared memory 承载双缓冲，解决 48 KiB 静态 shared 上限。
  - 在 `run(...)` 里设置 `cudaFuncAttributeMaxDynamicSharedMemorySize` 和 shared carveout。
- `cases/04_large_30k/build.sh`
  - 将构建目标收窄到 `code0.cu` 到 `code5.cu`，避免额外的 `code6.cu` 破坏构建。

## Commands

- `date '+%F %T %Z'`
- `sed -n '1,260p' cases/04_large_30k/code5.cu`
- `rg -n \"cp\\.async|memcpy_async|pipeline|wait_group|commit_group\" -S .`
- `/usr/local/cuda-13.1/bin/nvcc -c cases/04_large_30k/code5.cu -O2 -std=c++17 -arch=sm_80 -o /tmp/task2_code5.o`
- `/usr/local/cuda-13.1/bin/nvcc cases/04_large_30k/gen.cpp cases/04_large_30k/code5.cu -O2 -std=c++17 -arch=sm_80 -o /tmp/task2_main5`
- `./build.sh`

## Findings

- `cp.async` 双缓冲方案是可行的，但不能继续用静态 shared，双缓冲后的总 shared 超过 48 KiB 静态上限。
- 改成动态 shared 后，`code5.cu` 可以正常编译和链接。
- `build.sh` 原先会把意外出现的 `code6.cu` 一起编进来；收窄目标后，目录级构建恢复正常。

## Notes

- 没有跑 32k 规模的实际 benchmark，当前只做了编译、链接和目录级构建验证。
