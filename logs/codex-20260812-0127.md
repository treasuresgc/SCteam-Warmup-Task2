# Codex Log - 2026-08-12 01:27 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-12 01:27:00 CST
- End: 2026-08-12 01:27 CST

## Task

Remove register tiling from `cases/03_medium_5k/code2.cu` so this version tests pure CTA/shared-memory tiling.

## Files Changed

- `cases/03_medium_5k/code2.cu`
  - Removed the per-thread `16 x 4` output tile and the `double sum[TM][TN]` accumulator array.
  - Replaced it with one scalar `sum` per thread and one output element per thread.
  - Replaced the dynamic `128 x 128 x 32` shared-memory version with a static `8 x 32 x 32` CTA tile using the existing `32 x 8` block shape.
  - Removed the large dynamic shared-memory launch and the related `cudaFuncSetAttribute` call.
- `Readme.md`
  - Updated the `code2.cu` description to say this version uses `8 x 32 x 32` CTA tiling and no register tiling.

## Commands

- `/usr/local/cuda-13.1/bin/nvcc gen.cpp code2.cu -O2 -std=c++17 -arch=sm_80 -Xptxas=-v -o main2`
  - Succeeded.
  - ptxas reported `31 registers/thread`, `10240 bytes smem`, `0 bytes spill stores`, `0 bytes spill loads`, and `1` barrier.
- `date '+%Y%m%d-%H%M %Y-%m-%d %H:%M:%S %Z'`
  - Succeeded.

## Verification

- Build verification for `cases/03_medium_5k/main2` succeeded with CUDA 13.1 nvcc.
- Runtime benchmark was not executed because the current sandbox does not expose CUDA runtime device nodes.

## Notes

- The previous `128 x 128 x 32` version required per-thread multi-output accumulation with `32 x 8` threads, because a CTA has only `256` threads but `128 x 128 = 16384` output elements.
- Without register tiling, the output tile must match the number of output elements covered by the block. With `BlockDim=(32, 8)`, that is `8 x 32`.
