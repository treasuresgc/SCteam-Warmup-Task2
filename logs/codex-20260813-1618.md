# Codex Log - 2026-08-13 16:18 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-13 16:18 CST
- End: 2026-08-13 16:18 CST

## Task

Move the `128 x 128 x 32` large-CTA kernel into `cases/03_medium_5k/code3.cu` and update the docs to match.

## Files Changed

- `cases/03_medium_5k/code3.cu`
  - Replaced the old `32 x 32` `2 x 2` per-thread tile with a `128 x 128 x 32` CTA tile.
  - Uses a `32 x 8` thread block.
  - Each thread accumulates a `16 x 4` output tile.
  - Uses dynamic shared memory for `A` and `B` tiles and sets `cudaFuncAttributeMaxDynamicSharedMemorySize`.
- `Readme.md`
  - Updated the `code3.cu` description so `03_medium_5k` now maps to the large CTA tile version.
  - Clarified that `04_large_30k/code3.cu` still uses the `32 x 32` tile with `2 x 2` per-thread output.

## Commands

- `sed -n '1,260p' cases/03_medium_5k/code3.cu`
- `rg -n "code2|code3|128 x 128 x 32|8 x 32 x 32|2x2|2 x 2|shared memory|CTA" Readme.md cases/03_medium_5k cases/04_large_30k`
- `sed -n '1,240p' cases/03_medium_5k/build.sh`
- `sed -n '1,220p' logs/codex-20260812-0025.md`
- `/usr/local/cuda-13.1/bin/nvcc gen.cpp code3.cu -O2 -std=c++17 -arch=sm_80 -Xptxas=-v -o /tmp/task2_main3`

## Verification

- Build verification succeeded.
- `ptxas` reported `255 registers`, `1 barrier`, and `0 spill stores/loads`.

## Notes

- This kernel is still aligned-only: it assumes `m`, `n`, and `k` are divisible by `128`, `128`, and `32`.
- The register count is very high, so occupancy is likely limited even though the code compiles cleanly.
