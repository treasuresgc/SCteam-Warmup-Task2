# Codex Log - 2026-08-26 04:48 CST

## Agent

Codex GPT-5

## Time

- Start: 2026-08-26 04:34 CST
- End: 2026-08-26 04:48 CST

## Task

新建 `cases/04_large_30k/code5.cu`，在 `code4.cu` 基础上提供一个更快的实现路径。

## Files Changed

- `cases/04_large_30k/code5.cu`
  - 新增 row-major `cuBLASLt` fast path。
  - 使用 heuristic 选择算法，并允许最多 `512 MiB` workspace。
  - 如果 `cuBLASLt` 不可用或 heuristic 失败，则回退到 `cublasDgemm`。
- `cases/04_large_30k/build.sh`
  - 为 04 目录的所有构建增加 `-lcublasLt`，使 `main5` 可直接生成。
- `Readme.md`
  - 补充 `code5.cu` / `main5` 的说明。
- `AGENT.md`
  - 补充 `code5.cu` 是 `cuBLASLt` fast path 的说明。

## Files Read

- `cases/04_large_30k/code4.cu`
- `cases/04_large_30k/code0.cu`
- `cases/04_large_30k/build.sh`
- `Readme.md`
- `AGENT.md`
- `/usr/local/cuda-13.1/targets/x86_64-linux/include/cublasLt.h`
- `/usr/local/cuda-13.1/targets/x86_64-linux/include/cublas_api.h`
- `third_party/cutlass` repository tree

## Commands

- `nl -ba cases/04_large_30k/code4.cu | sed -n '1,260p'`
- `sed -n '1,220p' cases/04_large_30k/code0.cu`
- `git -C third_party/cutlass ls-tree -r --name-only HEAD | sed -n '1,200p'`
- `git -C third_party/cutlass cat-file -e HEAD:include/cutlass/cutlass.h`
- `sed -n '1000,1045p' /usr/local/cuda-13.1/targets/x86_64-linux/include/cublasLt.h`
- `sed -n '1120,1170p' /usr/local/cuda-13.1/targets/x86_64-linux/include/cublasLt.h`
- `sed -n '1340,1705p' /usr/local/cuda-13.1/targets/x86_64-linux/include/cublasLt.h`
- `sed -n '2260,2415p' /usr/local/cuda-13.1/targets/x86_64-linux/include/cublasLt.h`
- `./build.sh` in `cases/04_large_30k`
- `ls -l main0 main1 main2 main3 main4 main5`

## Findings

- `third_party/cutlass` 只有仓库对象树，没有展开的 `include/` 工作树；直接读取 `include/cutlass/cutlass.h` 会触发缺失 blob 的远端获取，而当前环境没有网络。
- 因此这次没有走真实 CUTLASS 编译路径，而是用了当前环境可编译的 `cuBLASLt` 优化路径。
- `code5.cu` 编译成功，`build.sh` 也成功生成了 `main5`。

## Notes

- `code5.cu` 不是手写 WMMA kernel，而是 library fast path。
- 如果后续要换成真正的 CUTLASS 版本，需要先把 CUTLASS 头文件完整展开到工作树里。
