# Codex Work Log

- Agent: Codex
- Start time: 2026-08-03 14:37 CST
- End time: 2026-08-03 14:37 CST

## Task

补充当前 GPU 的寄存器数量信息，回答“每个 core 的寄存器个数”。

## Files changed

- Updated `GPU_CONFIG.md`
- Added `logs/codex-20260803-1437.md`

## Commands run

- `sed -n '1,180p' GPU_CONFIG.md`
- `date '+%Y-%m-%d %H:%M:%S %Z'`

## Result

Updated `GPU_CONFIG.md` with Compute Capability 8.0 register limits:

- `65536` 32-bit registers per SM.
- `7077888` 32-bit registers per GPU across 108 SMs.
- `1024` 32-bit registers per FP32 CUDA core as a derived normalization.
- `255` maximum 32-bit registers per thread.

The document now explicitly notes that NVIDIA register files are allocated at SM level, so the per-core value is only a derived ratio and should not be used as the primary occupancy constraint.

## Validation

No build, CUDA runtime query, or profiling command was executed. This was a documentation-only update.

## Open issues

- The current sandbox still cannot directly run CUDA runtime device-property queries because `/dev/nvidia*` nodes are not visible.
